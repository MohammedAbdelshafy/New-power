# Runtime logs — written by every session, cause merge conflicts if tracked
.claude/whatsapp-log.json
.claude/hook-debug.log

# Never commit live credentials
.claude/whatsapp-config.local.json

# Generated lead CSVs — delivered to clients, not stored in repo
output/
